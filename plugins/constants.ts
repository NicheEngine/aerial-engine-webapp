/**
 * The name of the configuration file
 * entered the production environment
 */
export const APP_CONFIG_NAME = '_app.config.js'

export const APP_OUTPUT_NAME = 'dist'

export const APP_SERVER_PORT = 'VITE_SERVER_PORT'

export const APP_DEV_ENV_NAME = 'development'

export const APP_DEV_ENV_ALIAS = 'dev'

export const APP_PROD_ENV_NAME = 'production'

export const APP_PROD_ENV_ALIAS = 'prod'

export const APP_LOCAL_ENV_NAME = 'local'

export const APP_TEST_ENV_NAME = 'test'

export default {
  APP_CONFIG_NAME,
  APP_OUTPUT_NAME,
  APP_SERVER_PORT,
  APP_DEV_ENV_NAME,
  APP_DEV_ENV_ALIAS,
  APP_PROD_ENV_NAME,
  APP_PROD_ENV_ALIAS,
  APP_LOCAL_ENV_NAME,
  APP_TEST_ENV_NAME,
}
